{% extends 'base.html.twig' %}

{% block body %}

    <h1>{{ category }}</h1>
    {% for article in articles %}
        <h2>{{ article.title }}</h2>
        <i>{{ article.publicationDate | date('d/m/Y H:i:s')}}</i>
        <p class="text-right">
            <a href="{{ path('app_article_index', {id: article.id})  }}">More ...</a>
        </p>
        <hr>
        {% else %}
            <p>Aucun article dans cette catégorie</p>
    {% endfor %}

{% endblock %}
